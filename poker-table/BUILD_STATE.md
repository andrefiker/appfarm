# Build state

Baseline Stage 1 PASS reproduced. v1 server and PWA are being integrated around the immutable engine. Redis is intentionally omitted: one Railway server and one permanent table use Postgres transactions plus in-process recipient-specific WebSockets.
