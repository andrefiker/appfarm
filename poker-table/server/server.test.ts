import assert from 'node:assert/strict'; import test from 'node:test'; import { createGame, startHand } from '../engine/index.js'; import { observation, playerView, publicView } from './views.js'; import { decideBot } from './bot.js';
test('recipient views never expose another private hand or deck', () => { const s = startHand(createGame('HOLD_EM_NO_LIMIT', [{ id: 'a', seat: 0 }, { id: 'b', seat: 1 }], 0)); const owners = new Map([[0,{guestId:'a',name:'A',bot:false,connected:true}],[1,{guestId:'b',name:'B',bot:false,connected:true}]]); const pub = publicView('main', 1, s, owners, null, null); const a = playerView(pub,s,0); const spectator = playerView(pub,s,null); assert.equal(JSON.stringify(a).includes(s.players[1]!.hand[0]!.id), false); assert.equal(JSON.stringify(spectator).includes(s.players[0]!.hand[0]!.id), false); assert.equal(JSON.stringify(pub).includes('deck'), false); });
test('bot decision is deterministic for identical authorized observation', () => { const s = startHand(createGame('HOLD_EM_NO_LIMIT', [{ id:'a',seat:0},{id:'b',seat:1}],0)); const seat = s.players.find((p)=>p.id===s.actorId)!.seat; const o = observation(s,seat); assert.deepEqual(decideBot(o,'balanced',{next:()=>.5}),decideBot(o,'balanced',{next:()=>.5})); });
